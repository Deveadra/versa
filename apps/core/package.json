{
  "name": "@versa/core",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "echo \"lint ok\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@versa/config": "workspace:*",
    "@versa/database": "workspace:*",
    "@versa/environment": "workspace:*",
    "@versa/logging": "workspace:*",
    "@versa/memory": "workspace:*",
    "@versa/shared": "workspace:*",
    "@versa/workspaces": "workspace:*",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "zod": "^3.24.2"
  }
}
