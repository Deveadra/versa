{
  "name": "@versa/mcp-gateway",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "echo \"lint ok\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@versa/config": "workspace:*",
    "@versa/logging": "workspace:*",
    "@versa/shared": "workspace:*",
    "express": "^4.21.2"
  }
}
