version: "3.9"

services:
  aerith:
    build: .
    container_name: aerith_offline
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}   # optional for GPT
      # - OPENAI_API_KEY=env.OPENAI_API_KEY  # optional for GPT
    volumes:
      - .:/app
    stdin_open: true
    tty: true
