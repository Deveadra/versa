version: "3.9"

services:
  jarvis:
    build: .
    container_name: jarvis_offline
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}   # optional for GPT
      - OPENAI_API_KEY=env.OPENAI_API_KEY  # optional for GPT
    volumes:
      - .:/app
    stdin_open: true
    tty: true
